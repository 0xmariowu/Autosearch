# Control Plane

This directory stores the current AutoSearch operating program.

- `latest-program.json` is the merged runtime view of:
  - objective
  - source capability
  - runtime experience
  - query-family playbooks

This is the closest machine-readable equivalent of the lightweight
`program.md` idea from `karpathy/autoresearch`, but adapted for search work
instead of model-training work.
