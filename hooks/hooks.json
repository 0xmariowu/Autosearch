{
  "description": "AutoSearch plugin hooks",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ ! -d \"$HOME/.autosearch/venv\" ]; then echo '[AutoSearch] Setting up dependencies...'; bash \"${CLAUDE_PLUGIN_ROOT}/scripts/setup.sh\" 2>&1; else echo 'AutoSearch ready'; fi",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
